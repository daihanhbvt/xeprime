# XePrime docs index

Ngày dọn tài liệu: 22/07/2026

Thư mục này chỉ giữ các tài liệu chính cần dùng trước khi dựng source mới Next.js + NestJS.

## Bộ tài liệu nên đọc theo thứ tự

| Thứ tự | File | Mục đích |
| --- | --- | --- |
| 1 | `xeprime_screen_spec_by_role_before_db.docx` | Tổng hợp màn hình, role, chức năng theo từng role trước khi thiết kế DB |
| 2 | `xeprime_overall_user_flow_next_node_updated.docx` | User flow tổng thể cho customer, shop/host, platform admin |
| 3 | `xeprime_database_design.docx` | Thiết kế database MySQL/Prisma cho source mới |
| 4 | `xeprime_build_plan_nextjs_nestjs_prod.docx` | Plan build source mới Next.js + NestJS để lên production |
| 5 | `xeprime_fe_base_stack_calendar.docx` | FE base stack và thiết kế màn lịch thuê xe custom không dùng thư viện tính phí |
| 6 | `xeprime_product_base_source_master_prompt.docx` | Master prompt dựng base source product Next.js + NestJS |
| 7 | `xeprime_claude_code_handoff_prompt.docx` | Prompt bàn giao khi mở Claude Code/Cursor/VS Code không có lịch sử chat |
| 8 | `xeprime_claude_max_task_prompts.docx` | Prompt chia task cho Claude Max/code agent |

## File Markdown nguồn

Các file `.md` tương ứng được giữ lại để dễ sửa và xuất lại `.docx`.

## Tài liệu đã archive

Các tài liệu phân tích cũ, file HTML giải thích, roadmap cũ hoặc prompt lẻ đã được chuyển vào:

`_archive_2026_07_22/`

Không cần đọc thư mục archive nếu chỉ muốn triển khai source mới.

Ghi chú: `xeprime_database_design_prompt.docx` là file prompt lẻ đã được thay thế bởi `xeprime_claude_max_task_prompts.docx`, nhưng hiện Windows đang khóa file nên chưa chuyển vào archive được.
