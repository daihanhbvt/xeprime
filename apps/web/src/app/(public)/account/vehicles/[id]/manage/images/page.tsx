import { ImagesSection } from '@/features/vehicle-manage/components/sections/ImagesSection';

/** Mục "Hình ảnh" — nội dung thật nằm ở component dùng chung; trang chỉ là điểm vào theo URL. */
export default function Page() {
  return <ImagesSection />;
}
