import { HandoverTimeSection } from '@/features/vehicle-manage/components/sections/HandoverTimeSection';

/** Mục "Thời gian giao nhận" — nội dung thật nằm ở component dùng chung; trang chỉ là điểm vào theo URL. */
export default function Page() {
  return <HandoverTimeSection />;
}
