import { TripHistorySection } from '@/features/vehicle-manage/components/sections/TripHistorySection';

/** Mục "Lịch sử chuyến" — nội dung thật nằm ở component dùng chung; trang chỉ là điểm vào theo URL. */
export default function Page() {
  return <TripHistorySection />;
}
