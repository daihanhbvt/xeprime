'use client';

import { ShopSupportCasesPage } from '@/features/support-cases/components/ShopSupportCasesPage';

/** Thân trang ở `src/features/support-cases/components/ShopSupportCasesPage` — dùng chung với phiên hỗ trợ gian hàng (ADR 0050). */
export default function Page() {
  return <ShopSupportCasesPage />;
}
