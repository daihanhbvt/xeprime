'use client';

import { ShopPage } from '@/features/shop/components/ShopPage';

/** Thân trang ở `src/features/shop/components/ShopPage` — dùng chung với phiên hỗ trợ gian hàng (ADR 0050). */
export default function Page() {
  return <ShopPage />;
}
