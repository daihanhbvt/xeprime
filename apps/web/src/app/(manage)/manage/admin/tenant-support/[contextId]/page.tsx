import { SupportWorkspaceHome } from '@/features/tenant-support/components/SupportWorkspaceHome';

/** Trang đầu của phiên hỗ trợ — danh sách xe của gian hàng, hoặc trạng thái đăng ký. */
export default function SupportContextPage() {
  return <SupportWorkspaceHome />;
}
