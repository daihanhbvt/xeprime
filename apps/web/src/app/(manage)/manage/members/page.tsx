'use client';

import { MembersPage } from '@/features/members/components/MembersPage';

/** Thân trang ở `src/features/members/components/MembersPage` — dùng chung với phiên hỗ trợ gian hàng (ADR 0050). */
export default function Page() {
  return <MembersPage />;
}
