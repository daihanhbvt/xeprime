'use client';

import { DriversPage } from '@/features/drivers/components/DriversPage';

/** Thân trang ở `src/features/drivers/components/DriversPage` — dùng chung với phiên hỗ trợ gian hàng (ADR 0050). */
export default function Page() {
  return <DriversPage />;
}
