import type { components } from '@xeprime/types';

/** Shape từ OpenAPI (ADR 0007) — không viết tay lại. */
export type PublicBanner = components['schemas']['PublicBannerDto'];
export type AdminBanner = components['schemas']['AdminBannerDto'];
