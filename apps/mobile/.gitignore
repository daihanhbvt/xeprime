# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native — sinh bởi `expo prebuild`, không commit
/android
/ios
.kotlin/

# Tamagui — cache bóc tách style do `@tamagui/babel-plugin` sinh ra mỗi lần build; nội dung
# mang đường dẫn tuyệt đối của máy nên commit lên là mỗi người một bản.
# KHÔNG phải nguồn: `src/theme/tamagui.config.ts` mới là cấu hình, và nó vẫn được commit.
.tamagui/

# Metro
.metro-health-check*

/docs/
