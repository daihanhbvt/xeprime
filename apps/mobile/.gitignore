# Expo
.expo/
dist/
web-build/
expo-env.d.ts
# Khai báo route sinh tự động. Bản THẬT nằm ở `.expo/types/router.d.ts` (đã bỏ qua ở trên);
# gọi `regenerateDeclarations()` với sai thư mục gốc sẽ đẻ thêm một bản ở đây, mà tsconfig lại
# quét `**/*.ts` nên bản cũ đó âm thầm tham gia biên dịch.
/router.d.ts

# Native — sinh bởi `expo prebuild`, không commit
/android
/ios
.kotlin/

# Tamagui — cache bóc tách style do `@tamagui/babel-plugin` sinh ra mỗi lần build; nội dung
# mang đường dẫn tuyệt đối của máy nên commit lên là mỗi người một bản.
# KHÔNG phải nguồn: `src/theme/tamagui.config.ts` mới là cấu hình, và nó vẫn được commit.
.tamagui/

# Metro
.metro-health-check*

/docs/

# Credential Firebase cho thông báo đẩy — KHÔNG commit.
# Chúng khai project Firebase nào, và mỗi môi trường là một project khác nhau: một bản trong
# repo nghĩa là mọi bản build đều bắn thông báo vào cùng một nơi. Tải từ Firebase Console và
# đặt vào `credentials/` (xem docs/push-notifications.md §2).
/credentials/
google-services.json
GoogleService-Info.plist
