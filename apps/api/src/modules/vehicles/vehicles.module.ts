import { Module } from '@nestjs/common';

/**
 * Skeleton — Phase 2 implement CRUD xe, pricing, ảnh, giấy tờ, gửi duyệt public.
 *
 * Khi thêm chức năng khoá xe/bảo dưỡng: ghi lịch qua `OccupancyService` của
 * CalendarModule, KHÔNG tự INSERT vào `vehicle_occupancies` (ADR 0006).
 */
@Module({})
export class VehiclesModule {}
