node_modules/
.pnpm-store/

# build output
dist/
build/
.next/
out/
*.tsbuildinfo

# turbo
.turbo/

# env
.env
.env.*
!.env.example

# prisma generated client
prisma/generated/
prisma/.env

# test / coverage
coverage/
.nyc_output/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editor / os
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# claude: settings.json (plansDirectory…) commit; settings.local.json là allowlist cá nhân
.claude/settings.local.json

# secrets — never commit service account keys
*serviceAccount*.json
firebase-adminsdk-*.json

# git worktree phụ (agent) — không được để `git add -A` nuốt vào commit
.codex-worktrees/

# office lock file (~$Book.xlsx)
~$*
